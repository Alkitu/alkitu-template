# ============================================================================
# ALKITU TEMPLATE - WEB PACKAGE ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in your actual values
# ============================================================================

# Database Connection
DATABASE_URL="mongodb://localhost:27017/alkitu-dev?retryWrites=true&w=majority"

# Application URLs
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Environment
NODE_ENV="development"

# ============================================================================
# AUTHENTICATION BYPASS (DEVELOPMENT ONLY)
# ============================================================================
# WARNING: Setting SKIP_AUTH="true" bypasses ALL authentication middleware
#
# When SKIP_AUTH="true":
# - All protected routes become accessible without authentication
# - JWT validation is disabled
# - Role-based access control (RBAC) is bypassed
# - 3 E2E authentication tests will be SKIPPED:
#   1. ALI-119 Test #1: "Should require authentication to access requests"
#   2. ALI-119 Test #2: "Should require authentication to create request"
#   3. ALI-117 Test #10: "Should require authentication"
#
# Use cases:
# - SKIP_AUTH="true"  → Development mode (faster iteration, no login required)
# - SKIP_AUTH="false" → Testing mode (validates auth flows, runs all E2E tests)
# - Not set/removed   → Production mode (full authentication required)
#
# To run authentication E2E tests:
# 1. Set SKIP_AUTH="false" or remove this variable
# 2. Restart dev servers: npm run dev
# 3. Run tests: npx playwright test
#
# IMPORTANT: NEVER set SKIP_AUTH in production environments!
# ============================================================================
# SKIP_AUTH="false"

# ============================================================================
# OAUTH PROVIDERS (Optional)
# ============================================================================
# GitHub OAuth
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ============================================================================
# EMAIL SERVICE (Resend)
# ============================================================================
RESEND_API_KEY="re_your_resend_api_key"
AUTH_RESEND_KEY="re_your_resend_api_key"
EMAIL_FROM="noreply@yourdomain.com"

# ============================================================================
# EXTERNAL INTEGRATIONS (Optional)
# ============================================================================
# Google Account for automation
GOOGLE_USER_EMAIL="your-email@gmail.com"
GOOGLE_USER_PASSWORD="your-app-password"

# Holded API
HOLDED_API_KEY="your-holded-api-key"

# Google Drive API
GOOGLE_DRIVE_CLIENT_EMAIL="your-service-account@your-project.iam.gserviceaccount.com"
GOOGLE_DRIVE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----"
GOOGLE_DRIVE_PROJECT_ID="your-project-id"
GOOGLE_DRIVE_CLIENT_ID="your-client-id"
GOOGLE_DRIVE_ROOT_FOLDER_ID="your-folder-id"
