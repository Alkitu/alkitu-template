export interface ServiceIconProps {
  /**
   * Service category name to determine which icon to display
   */
  category: string;

  /**
   * Additional CSS classes for styling
   */
  className?: string;
}
